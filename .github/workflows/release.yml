name: Build & Release

on:
  push:
    tags:
      - 'v[0-9]+\.[0-9]+\.[0-9]+'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
    - uses: actions/checkout@v4

    - id: nvmrc
      uses: browniebroke/read-nvmrc-action@v1

    - uses: actions/setup-node@v4
      with:
        node-version: '${{ steps.nvmrc.outputs.node_version }}'
        cache: 'npm'

    - run: npm ci

    - name: Set ASSET_SUBPATH Environment Variable for GitHub Pages Deploy
      if: runner.os == 'Linux' && github.ref_name && startsWith(github.ref, 'refs/tags/')
      run: |
        echo "GH_PAGES_URL=https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/" >> $GITHUB_ENV
        echo "ASSET_SUBPATH=/${{ github.event.repository.name }}/" >> $GITHUB_ENV

    - run: npm run build
      env:
        GH_PAGES_URL: ${{ env.GH_PAGES_URL }}
        ASSET_SUBPATH: ${{ env.ASSET_SUBPATH }}

    - name: Upload GitHub Pages artifact
      if: runner.os == 'Linux'
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build, deploy]
    permissions:
      contents: write

    steps:
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          body: |
            Release ${{ github.ref_name }}
            See the deployed page at: ${{ github.event.repository.html_url }}/pages/
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

